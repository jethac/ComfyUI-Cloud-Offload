# ComfyUI-Cloud-Offload talks to the standalone Cloud Offload coordinator over
# HTTP. It does not import the coordinator (cloud_offload) as a Python library.
#
# torch, numpy, and Pillow are provided by the host ComfyUI install.
requests>=2.28
safetensors>=0.4
